# Fireworks AI — cloud report/synthesis model (the 3rd, cloud-side model).
# Required for the live AI report; a rule-based fallback runs without it.
FIREWORKS_API_KEY=your_fireworks_api_key_here
FIREWORKS_BASE_URL=https://api.fireworks.ai/inference/v1
# Any chat model your Fireworks account can serve. Verify with
# `python scripts/fireworks_live_check.py`. (Legacy alias: GEMMA_MODEL.)
REPORT_MODEL=accounts/fireworks/models/deepseek-v4-pro
# Structured-JSON mode — keep on so reasoning models return a clean report object.
REPORT_JSON_MODE=true

# MedGemma (local vLLM) - Optional, regex fallback available
MEDGEMMA_BASE_URL=http://localhost:8001/v1
MEDGEMMA_MODEL=google/medgemma-4b-it

# TxGemma (local vLLM) - Optional, database fallback available
TXGEMMA_BASE_URL=http://localhost:8002/v1
TXGEMMA_MODEL=google/txgemma-2b-it

# Feature flags — default UI toggle states.
# CPU-only setups keep the local vLLM models off (regex parser + rules DB
# still cover parsing and DDI checks); set the first two to true on a GPU pod.
USE_LLM_PARSER=false
USE_TXGEMMA=false
USE_GEMMA4=true
